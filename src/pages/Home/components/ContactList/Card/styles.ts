import { CSS, styled } from '../../../../../../stitches.config';

const Container = styled('div', {} as CSS);

const ContentArea = styled('div', {} as CSS);

const ContentRow = styled('div', {} as CSS);

const ContactName = styled('strong', {} as CSS);

const ContactSocialMedia = styled('span', {} as CSS);

const ContactMail = styled('span', {} as CSS);

const ContactPhone = styled('span', {} as CSS);

const ButtonEdit = styled('button', {} as CSS);

const ButtonRemove = styled('button', {} as CSS);

const ButtonImage = styled('img', {} as CSS);

export {
  Container,
  ButtonImage,
  ButtonRemove,
  ButtonEdit,
  ContactPhone,
  ContactMail,
  ContactName,
  ContactSocialMedia,
  ContentArea,
  ContentRow,
};
